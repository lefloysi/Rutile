if(RUTILE_BUILD_VULKAN)
    add_subdirectory("rt-vulkan")
endif()
if(RUTILE_BUILD_DX12 AND WIN32)
    add_subdirectory("rt-dx12")
endif()
if(RUTILE_BUILD_GL33 AND WIN32)
    add_subdirectory("rt-gl33")
endif()