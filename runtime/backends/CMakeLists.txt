if(RUTILE_BUILD_VULKAN)
    add_subdirectory("rt-vulkan")
endif()
if(RUTILE_BUILD_DIRECTX12 AND WIN32)
    add_subdirectory("rt-directx12")
endif()
# if(RUTILE_BUILD_OPENGL AND WIN32)
#     add_subdirectory("rt-opengl")
# endif()
